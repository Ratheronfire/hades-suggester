import { EnabledForSelectingPipe } from './enabled-for-selecting.pipe';

describe('EnabledForSelectingPipe', () => {
  it('create an instance', () => {
    const pipe = new EnabledForSelectingPipe();
    expect(pipe).toBeTruthy();
  });
});
